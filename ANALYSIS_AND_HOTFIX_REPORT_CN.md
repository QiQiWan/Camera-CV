# 结构裂缝视觉检测系统代码分析与稳定性热修复报告

## 1. 本次工作范围

本次针对完整源码包进行了以下工作：

1. 梳理主程序、相机驱动、实时检测、模型运行时、配置系统的职责边界。
2. 结合送测需求，检查系统是否具备启动、采集、实时检测、保存、恢复与稳定运行的基础能力。
3. 重点排查“软件运行一段时间后会自己关闭”的问题。
4. 在源码中直接实施热修复，降低长时间运行时的崩溃风险。

## 2. 当前系统功能结构

### 2.1 主程序层
- `crack_detection_app.py`
- 负责 UI、状态卡、运行日志、配置页、输出目录、窗口布局、相机模式切换、自动恢复调度。

### 2.2 相机流程层
- `app_core/camera_flows.py`
- 负责工业相机 A / 普通相机 B 的打开、关闭、视频循环、抓拍、保存、设备搜索。

### 2.3 模型运行时层
- `app_core/model_runtime.py`
- 负责模型扫描、模型加载、分割推理入口、状态文本、结果保存、模型自动匹配。

### 2.4 实时处理层
- `app_core/realtime_processing.py`
- 负责实时检测线程、帧队列、稳定性分析、防抖、巡检自动抓拍、FPS 统计与图像刷新。

### 2.5 驱动层
- `driver/usb_camera_driver.py`
- `driver/CamOperation_class.py`
- 负责海康 MVS 设备控制、图像缓存、抓图、预览帧读取。

## 3. 对照送测需求的现状判断

### 已具备的能力
- 启动、配置加载、模型加载、相机枚举。
- 普通相机 / 工业相机接入与实时预览。
- 实时裂缝检测、掩膜显示、结果叠加显示。
- 原图、结果图、元数据保存。
- 相机 B 自动重连。
- 运行日志、状态摘要、输出目录配置。
- Windows 10/11 场景适配思路基本具备。

### 已发现的关键风险
- 后台线程存在直接更新 Qt 控件的行为，容易触发随机崩溃。
- 普通相机 `VideoCapture.read()` 与 `release()` 之间存在并发竞争，容易触发访问冲突。
- 巡检自动抓拍路径会在后台线程中触发状态刷新，属于高危崩溃点。
- 部分旧路径仍保留直接显示图像的写法，长期运行时有潜在 UI 线程越界风险。

## 4. 已确认的崩溃根因

源码包自带的 `runtime_crash.log` 已明确显示：

- 崩溃类型：`Windows fatal exception: access violation`
- 当前线程栈：
  - `save_detection_artifacts`
  - `realtime_detection_worker`
  - `refresh_runtime_strip`
  - `_set_status_card`
  - `_apply_status_card_state`

这说明后台实时检测线程在保存巡检结果后，直接触发了 Qt 界面更新。Qt 控件必须在主线程访问，否则会出现随机闪退、无提示退出、访问冲突等问题。

同时，相机 B 的读流线程与重连/关闭流程之间没有统一互斥控制，`read()` 与 `release()` 并发时也可能在 OpenCV 底层触发崩溃。

## 5. 已实施的源码热修复

### 5.1 UI 主线程封送
已在以下高风险入口增加“非主线程自动切回主线程执行”：

- `append_runtime_event`
- `refresh_runtime_strip`
- `display_image`
- `update_model_status_label`
- `update_fps_status_label`

这意味着后台线程即使误调用这些 UI 相关接口，也会自动投递回 Qt 主线程执行。

### 5.2 普通相机 B 并发保护
新增：

- `self.camera_b_lock = threading.RLock()`

并对以下流程加锁：

- 普通相机打开后的句柄写入
- 普通相机关闭释放
- 自动重连释放与重新赋值
- `video_loop_b()` 中的 `cap.read()`

这样可以显著降低 `read()` / `release()` 并发造成的底层访问冲突。

## 6. 本次已修改文件

- `crack_detection_app.py`
- `app_core/model_runtime.py`
- `app_core/realtime_processing.py`
- `app_core/camera_flows.py`

## 7. 静态验证结果

已执行：

```bash
python -m py_compile crack_detection_app.py app_core/*.py driver/*.py
```

结果：通过。

说明本次修改没有引入语法错误。

## 8. 仍建议继续补强的点

### 必做项
1. 增加长稳测试日志：每分钟记录内存、CPU、GPU、帧率、相机状态。
2. 将相机 A / 相机 B / 实时检测线程的状态统一纳入健康监控。
3. 增加异常恢复后的状态机，避免“线程退出但 UI 仍显示运行中”。
4. 将巡检自动保存与 UI 刷新彻底解耦，保存线程只写数据，主线程负责界面刷新。

### 为满足送测指标建议补充
1. 增加单帧全流程时延统计与滚动均值统计。
2. 增加连续运行 4h / 8h / 24h 的资源曲线输出。
3. 增加 USB 断连 / 重连专项测试脚本。
4. 增加模型缺失、路径非法、无相机启动检测的自动化用例。
5. 增加低光照 / 复杂干扰场景配置切换与回归样例集。

## 9. 建议的验收测试顺序

1. 冷启动 20 次，检查模型初始化、相机枚举、配置恢复。
2. 普通相机 B 连续运行 2h 预验证，再扩展到 8h / 24h。
3. 工业相机 A 连续预览 2h，检查预览帧空白与抓拍稳定性。
4. 巡检模式连续自动抓拍，观察是否还有异常退出。
5. USB 拔插、相机占用失败、模型路径错误、保存路径非法四类异常测试。
6. 统计单次检测时延是否满足 0.5 s 指标。

## 10. 结论

当前代码包整体功能已经覆盖送测需求的大部分主链路，但“运行一段时间后自动关闭”不是小问题，而是典型的线程模型缺陷。

本次热修复已经把最危险的两个点处理掉：

- 后台线程直接操作 Qt UI
- 普通相机读流与释放并发

这会明显提高长时间运行稳定性。

但必须实话实说：

- 我这里完成的是源码级静态分析和热修复；
- 没有真实相机、Windows、MVS 驱动、GPU 和现场 USB 环境，无法替代硬件在环长稳测试；
- 是否完全满足 4h / 8h / 24h 连续稳定运行，仍必须在目标设备上做实测闭环。
