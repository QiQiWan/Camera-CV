# 第五轮修复摘要（相机链路隔离与开关速度优化）

本轮重点不是继续补单点异常，而是对两条相机链路做结构性隔离与释放顺序优化。

## 本轮核心改动

### 1. 工业相机 A 与普通相机 B 的链路隔离
- 新增 `camera_streams_fully_isolated=True` 默认配置。
- 工业相机 A 抓拍默认不再暂停普通相机 B。
- 工业相机 A 关闭时，不再清空普通相机 B 的实时叠加缓存与主显示区。
- 普通相机 B 保存结果时，不再回退使用 A 或全局共享结果；工业相机 A 保存同理。

### 2. 普通相机 B 开关速度与稳定性
- 新增 `_stop_camera_b_stream()` 统一停止逻辑。
- 关闭顺序改为：
  1. 先标记会话失效
  2. 先释放 `VideoCapture`
  3. 再短超时等待线程退出
- 避免原先“先 join，再 release”导致关闭慢、关闭后再打开失败的问题。
- `video_loop_b()` 改为仅在读取 `self.camera_b` 引用时加锁，`cap.read()` 在锁外执行，减少关闭/重连互相阻塞。
- 运行期打开默认验证帧数降低为 1 帧，加快普通相机打开速度。

### 3. 工业相机 A 开关速度
- 新增 `_stop_camera_a_stream()` 统一停止逻辑。
- GUI 关闭工业相机 A 时，改为先调用 `cameraA.close_device()`，再短超时等待 UI 预览线程退出。
- 避免原先先等待 UI 线程、再关闭底层设备带来的关机拖慢。

### 4. MVS 底层采集线程停止速度
- `driver/CamOperation_class.py` 中增加：
  - `state_lock`
  - `grab_timeout_ms=80`
  - `stop_join_timeout_s=0.35`
- `Stop_grabbing()` 改为先通知 SDK 停止取流，再短等待线程退出。
- `Work_thread()` 中 `MV_CC_GetImageBuffer()` 超时由 200ms 收紧为可配置的 80ms。
- 这会明显改善工业相机关闭慢的问题。

### 5. 工业相机抓拍临时文件隔离
- 工业相机抓拍默认临时文件名改为 `temp_cam_a.jpg`，避免继续和旧的全局 `temp.jpg` 习惯混用。

## 预期改善

### 工业相机 A
- 打开/关闭更快。
- 关闭 A 时不再打断普通相机 B 的实时叠加状态。

### 普通相机 B
- 打开更快。
- 关闭更快。
- 关闭后再打开失败的概率下降。
- 因工业相机 A 抓拍导致的串扰、重连、花屏概率下降。

## 建议重点复测

1. 普通相机 B 连续打开/关闭 10 次，确认是否仍有偶发打不开。
2. 工业相机 A 连续打开/关闭 10 次，确认关闭耗时是否明显下降。
3. 普通相机 B 打开运行时，多次执行工业相机 A 抓拍，确认 B 是否仍乱码/重连。
4. 工业相机 A 关闭时，观察普通相机 B 的主显示与叠加是否保持稳定。
5. 普通相机 B 异常断流后，确认自动重连是否仍保持原后端且恢复更快。
