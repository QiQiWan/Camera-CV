# UI 优化空间分析（P6.7-performance-ui-hotfix11）

## 一、本轮新增优化

### 1. 配置面板支持搜索定位
之前配置中心虽然已经拆分为多页，但当用户明确知道自己要找“激光偏移”“ONNX 模型”“保存目录”时，仍然需要肉眼逐页翻找。

这一版新增：
- 顶部搜索框
- 搜索结果自动过滤左侧导航
- 自动跳转到首个匹配页面
- `Ctrl+F` 快捷聚焦
- 关键词提示区

这使配置面板从“按页浏览”升级为“按意图直达”。

### 2. 放大预览窗口从“全速刷新”改为“限频刷新”
之前一旦打开放大预览窗口，Qt 会持续对更大尺寸图像做缩放和重绘。
这会带来两个问题：
- 主界面实时预览掉帧
- 窗口交互时更容易感觉发飘或迟滞

本轮改成可配置的限频更新后：
- 放大窗口仍保持动态
- 但不会和主预览抢太多渲染预算

### 3. 高频状态摘要改为合并刷新
原先实时检测链路会非常频繁地触发模型状态区、配置摘要区、运行摘要条的联动刷新。

这一版改为：
- 对高频实时状态做短间隔合并
- 重要配置/异常/成功信息仍即时显示

这样兼顾了：
- 实时反馈
- UI 稳定性
- 主线程负担控制

## 二、当前界面改进后的结构特点

现在界面的信息层次更清晰地分成三层：

1. **顶部状态层**
   - 状态卡
   - 运行摘要
   - 最近保存信息

2. **中间工作层**
   - 实时预览
   - 掩膜/分析
   - 最终结果

3. **侧边控制层**
   - 设备控制
   - 模型配置入口
   - 保存与日志

配置面板则进一步提供了“搜索式定位”，减少控制层过深导致的认知负担。

## 三、后续仍值得继续推进的方向

### 1. 配置项页内高亮
目前已经能定位到页面，下一步可以考虑：
- 在页面中高亮命中的关键词
- 自动滚动到相关控件附近

### 2. 运行状态分级显示
可以继续把状态分为：
- 常规信息
- 需要注意
- 异常阻断
- 已恢复

并提供不同颜色或 icon，让排查更快。

### 3. 结果面板支持“一键对比最近两次结果”
现场常见需求是比较：
- 本次抓拍 vs 上次抓拍
- 当前场景参数 vs 上次参数

后续可以加一个轻量对比视图。
